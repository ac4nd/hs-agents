<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hypersense.boot.system.mapper.LlmVendorConfigMapper">
    <select id="getVendorConfigPage" resultType="com.hypersense.boot.system.model.vo.LlmVendorConfigVO">
        SELECT
            t1.id,
            t1.vendor_name,
            t1.vendor_code,
            t1.config_key,
            t1.is_coding_plan,
            t1.access_level,
            t1.base_url,
            t1.available_quota,
            t1.used_quota,
            t1.quota_unit,
            t1.status,
            t1.sort,
            t1.remark,
            t1.create_time
        FROM sys_llm_vendor_config t1
        <where>
            t1.is_deleted = 0
            <if test='queryParams.keywords != null and queryParams.keywords.trim() neq ""'>
                AND (
                    t1.vendor_name LIKE '%' || #{queryParams.keywords} || '%'
                    OR t1.vendor_code LIKE '%' || #{queryParams.keywords} || '%'
                )
            </if>
            <if test='queryParams.status != null'>
                AND t1.status = #{queryParams.status}
            </if>
        </where>
        ORDER BY t1.sort, t1.create_time DESC
    </select>
</mapper>
